<Window x:Class="XHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XHelper"
        mc:Ignorable="d"
        Title="MainWindow" WindowStartupLocation="CenterScreen" TextOptions.TextFormattingMode="Display" UseLayoutRounding="False" Width="1600" Height="1000">
        <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Style/CommonStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel LastChildFill="True" Margin="10">
        <DockPanel DockPanel.Dock="Right" Width="500" Margin="5,0,0,0">
            <Border DockPanel.Dock="top">
                <DockPanel >

                </DockPanel>
            </Border>
        </DockPanel>
        <DockPanel DockPanel.Dock="Right">
            <Border DockPanel.Dock="top" Padding="20,10">
                <DockPanel Height="50" LastChildFill="False">
                    <GroupBox Style="{DynamicResource CommandGroup}" Header="SYSTEM" DockPanel.Dock="Left" Padding="0,5" Margin="0">
                        <WrapPanel>
                            <Button Style="{DynamicResource IconPathButton}" Tag="{StaticResource IconQuit}" x:Name="bnQuit" Command="ApplicationCommands.Close" ToolTip="Quit Application" DockPanel.Dock="Left" Width="30" Height="30" Margin="0,0,10,0" />
                            <Button Style="{DynamicResource IconPathButton}" Tag="{StaticResource IconResolve}" x:Name="bnOption" Command="ApplicationCommands.Close" ToolTip="Application Options" DockPanel.Dock="Left" Width="30" Height="30" Margin="0" />
                        </WrapPanel>
                    </GroupBox>
                    <GroupBox Style="{DynamicResource CommandGroup}" Header="ELEMENT" DockPanel.Dock="Left" Padding="0,5" Margin="20,0,0,0">
                        <WrapPanel>
                            <Button Style="{DynamicResource IconPathButton}" Tag="{StaticResource IconClassify}" x:Name="bnElementClassify" ToolTip="" DockPanel.Dock="Left" Width="30" Height="30" Margin="0,0,10,0" />
                            <Button Style="{DynamicResource IconPathButton}" Tag="{StaticResource IconResolve}" x:Name="bnElementResolve" ToolTip="" DockPanel.Dock="Left" Width="30" Height="30" Margin="0" />
                        </WrapPanel>
                    </GroupBox>
                    <GroupBox Style="{DynamicResource CommandGroup}" Header="DATA" DockPanel.Dock="Left" Padding="0,5" Margin="20,0,0,0">
                        <WrapPanel>
                            <Button Style="{DynamicResource IconPathButton}" Tag="{StaticResource IconLoadData}" x:Name="bnLoadData" ToolTip="加载数据" DockPanel.Dock="Left" Width="30" Height="30" Margin="0,0,10,0" />
                            <Button Style="{DynamicResource IconPathButton}" Tag="{StaticResource IconSaveData}" x:Name="bnSaveData" ToolTip="保存修改" DockPanel.Dock="Left" Width="30" Height="30" Margin="0,0,10,0" />
                            <Button Style="{DynamicResource IconPathButton}" Tag="{StaticResource IconApplyParamData}" x:Name="bnApplyParameters" ToolTip="" DockPanel.Dock="Left" Width="30" Height="30" Margin="0" />
                        </WrapPanel>
                    </GroupBox>
                </DockPanel>
            </Border>
            <Border DockPanel.Dock="top" Margin="0,5,0,0">
                <DockPanel Margin="10" LastChildFill="True">
                    <DockPanel DockPanel.Dock="Left" Margin="10" LastChildFill="False">
                        <WrapPanel DockPanel.Dock="Top" Margin="0,10,0,0">
                            <Label Content="当前处理：" />
                        </WrapPanel>
                        <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Left">
                            <ProgressBar Width="300" Height="10" Value="88" OverridesDefaultStyle="true" RenderTransformOrigin="0,0" Margin="0" />
                        </StackPanel>
                        <WrapPanel DockPanel.Dock="Top" Margin="0,10,0,0">
                            <Label Content="总体进度：" />
                        </WrapPanel>
                        <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Left">
                            <ProgressBar Width="300" Height="10" Value="100" OverridesDefaultStyle="true" RenderTransformOrigin="0,0" Margin="0" />
                        </StackPanel>
                    </DockPanel>
                    <DockPanel DockPanel.Dock="Left" Margin="10" LastChildFill="True">
                        <ListBox x:Name="listInformation" SelectionChanged="listInformation_SelectionChanged" Style="{DynamicResource MonitorScreen}" ItemContainerStyle="{DynamicResource MonitorItem}" />
                    </DockPanel>
                </DockPanel>
            </Border>
            <Border DockPanel.Dock="top" Margin="0,5,0,0">
                <StackPanel Height="150" Orientation="Vertical"  Margin="10">
                    <Label Content="Stars List:" />
                    <ListBox Height="96" HorizontalAlignment="Left" Margin="10,0" Name="listBox2" VerticalAlignment="Top" SnapsToDevicePixels="True">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="30 0 30 0" />
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBoxItem Content="Vertical Item 1" />
                        <ListBoxItem Content="Vertical Item 2" />
                        <ListBoxItem Content="Vertical Item 3" />
                        <ListBoxItem Content="Vertical Item 4" />
                        <ListBoxItem Content="Vertical Item 5" />
                        <ListBoxItem Content="Vertical Item 5" />
                        <ListBoxItem Content="Vertical Item 5" />
                    </ListBox>
                </StackPanel>
            </Border>
            <Border DockPanel.Dock="top" Margin="0,5,0,0">
                <StackPanel Height="200"></StackPanel>
            </Border>
        </DockPanel>
    </DockPanel>
</Window>
